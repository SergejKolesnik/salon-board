name: salon_board_mobile
description: Read-only offline prototype for Salon Board Mobile v2.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^6.1.1
  dio: ^5.7.0
  intl: ^0.20.1
  path: ^1.9.0
  shared_preferences: ^2.3.3
  sqflite: ^2.4.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
